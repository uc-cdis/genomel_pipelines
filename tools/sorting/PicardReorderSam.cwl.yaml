#!/usr/bin/env cwl-runner

cwlVersion: "cwl:draft-3"

class: CommandLineTool

requirements:
  - $import: ../envvar-global.cwl
  - class: InlineJavascriptRequirement
  - class: ShellCommandRequirement
  - class: MultipleInputFeatureRequirement
  - class: DockerRequirement
    dockerPull: registry.gitlab.com/uc-cdis/genomel-primary-analysis:0.1d

inputs:
  - id: java_opts
    type: string
    default: "-Xmx16g"
    description: |
      "JVM arguments should be a quoted, space separated list (e.g. \"-Xmx8g -Xmx16g -Xms128m -Xmx512m\")"
    inputBinding:
      position: 1
      shellQuote: false
  - id: picard_jar_path
    type: string
    default: "/home/ubuntu/tools/picard-tools/picard.jar"
    inputBinding:
      position: 2
      prefix: "-jar"
  - id: picard_tool
    type: string
    default: "ReorderSam"
    inputBinding:
      position: 3
  - id: input_bam_path
    type: File
    inputBinding:
      position: 4
      prefix: "INPUT="
      separate: false
  - id: remove_discordance
    type: string
    default: "true"
    inputBinding:
      position: 6
      prefix: "S="
      separate: false
  - id: reference_seq
    type: File
    inputBinding:
      position: 7
      prefix: "REFERENCE="
      separate: false
    secondaryFiles:
      - "^.dict"
      - ".fai"
  - id: create_index
    type: string
    default: "true"
    inputBinding:
      position: 8
      prefix: "CREATE_INDEX="
      separate: false

outputs:
  - id: reordered_output_bam
    type: File
    outputBinding:
      glob: $(inputs.input_bam_path.path.replace(/^.*[\\\/]/, '').replace(/\.[^/.]+$/, '') + '.ro.bam')
    secondaryFiles:
      - "^.bai"

baseCommand: java
arguments:
  - valueFrom: $(inputs.input_bam_path.path.replace(/^.*[\\\/]/, '').replace(/\.[^/.]+$/, '') + '.ro.bam')
    position: 5
    prefix: "OUTPUT="
    separate: false

description: |
  This module uses Picard to sort BAM file
